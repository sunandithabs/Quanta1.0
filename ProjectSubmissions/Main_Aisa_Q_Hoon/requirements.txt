qiskit==0.44.0       # Quantum computing library for creating and simulating quantum circuits
qiskit-aer==0.12.0   # Qiskit module for high-performance quantum simulations
matplotlib==3.8.2    # Library for visualizing quantum circuits and results
numpy==1.26.2        # Numerical computing library for handling arrays and mathematical operations
📌 Explanation of Dependencies:
qiskit → The main Qiskit library for quantum computing.
qiskit-aer → Provides high-performance quantum simulation capabilities.
matplotlib → Used to visualize quantum circuits (qc.draw('mpl')).
numpy → Handles mathematical calculations like rotations (np.pi / 4).
📥 How to Install Dependencies?
Run the following command in your project folder:

bash
Copy
Edit
pip install -r requirements.txt
This ensures all required libraries are installed with the specified versions, preventing compatibility issues. 🚀
